<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:padding="10dp"
    android:background="#50000000"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ListView
        android:id="@+id/view_bottom_pop_listView"
        android:layout_above="@+id/view_bottom_pop_bottomLyt"
        android:cacheColorHint="@android:color/transparent"
        android:divider="@color/colorAccent"
        android:dividerHeight="1dp"
        android:listSelector="@android:color/transparent"
        android:paddingEnd="12dp"
        android:paddingStart="12dp"
        android:scrollbars="none"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <LinearLayout
        android:id="@+id/view_bottom_pop_bottomLyt"
        android:layout_alignParentBottom="true"
        android:layout_marginTop="8dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="48dp">

        <TextView
            android:id="@+id/view_bottom_pop_cancelText"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:gravity="center"
            android:lines="1"
            android:text="取消"
            android:textColor="@color/colorAccent"
            android:textSize="18sp"
            android:layout_height="48dp" />

        <TextView
            android:id="@+id/view_bottom_pop_confirmText"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:gravity="center"
            android:lines="1"
            android:text="取消"
            android:textColor="@color/colorAccent"
            android:textSize="18sp"
            android:visibility="gone"
            android:layout_height="48dp" />
    </LinearLayout>
</RelativeLayout>